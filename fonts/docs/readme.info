the /fonts contain files of script that was was manually added (i moved to autmoated system) and a config json file of what page should get what font (i think it will not be used no longe because i made a <cfont> flag)

Remote File Transfer Use Guide

# font_uploader.html
Uploaded font files and zip are base64 encoded and put to remoteupload folder wich their name and id
id raw_url etc is put to array.list

# font_sync.html
get fonts data from array.list
and put them into standalone scripts that when executed will change page font
it also because i want to it to do that make a big array of font

# fonts_to_ttf.html
read name
